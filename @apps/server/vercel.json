{
	"version": 2,
	"builds": [
		{
			"src": "package.json",
			"use": "@now/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "https://testapptotally.vercel.app/$1"
		}
	],
	"build": {
		"env": {
			"CI": "true"
		},
		"command": "npm run deploy:production"
	}
}
